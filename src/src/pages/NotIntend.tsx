import React from "react";

const NotIntend = () => {
  return (
    <div className="flex items-center justify-center">
      <p className="text-white">
        Sorry, this web application is for desktop only :(
      </p>
    </div>
  );
};

export default NotIntend;
